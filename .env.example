# RAT Logging Configuration
# Copy this file to .env and modify as needed

# Set log level for the entire application
# Options: error, warn, info, debug, trace
# This overrides the -v CLI flags
RUST_LOG=debug

# Set log level for specific modules
# Examples:
# RUST_LOG=rat=trace,agent_client_protocol=debug
# RUST_LOG=rat::acp=trace,rat::app=info

# For ACP (Agent Client Protocol) debugging
# RUST_LOG=agent_client_protocol=trace

# For maximum verbosity (all modules)
# RUST_LOG=trace
